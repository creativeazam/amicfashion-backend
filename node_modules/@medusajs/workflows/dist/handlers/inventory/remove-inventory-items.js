"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeInventoryItems = void 0;
async function removeInventoryItems({ container, context, data, }) {
    const { manager } = context;
    const inventoryService = container.resolve("inventoryService");
    if (!inventoryService) {
        const logger = container.resolve("logger");
        logger.warn(`Inventory service not found. You should install the @medusajs/inventory package to use inventory. The 'removeInventoryItems' will be skipped.`);
        return;
    }
    return await inventoryService.deleteInventoryItem(data.inventoryItems.map(({ inventoryItem }) => inventoryItem.id), { transactionManager: manager });
}
exports.removeInventoryItems = removeInventoryItems;
removeInventoryItems.aliases = {
    inventoryItems: "inventoryItems",
};
//# sourceMappingURL=remove-inventory-items.js.map