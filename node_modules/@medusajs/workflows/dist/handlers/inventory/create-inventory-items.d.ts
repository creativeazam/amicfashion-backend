import { InventoryItemDTO, ProductTypes } from "@medusajs/types";
import { WorkflowArguments } from "../../helper";
type Result = {
    variant: ProductTypes.ProductVariantDTO;
    inventoryItem: InventoryItemDTO;
}[];
export declare function createInventoryItems({ container, context, data, }: WorkflowArguments<{
    products: ProductTypes.ProductDTO[];
}>): Promise<Result | void>;
export declare namespace createInventoryItems {
    var aliases: {
        products: string;
    };
}
export {};
